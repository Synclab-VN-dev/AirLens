// TECH DEBT: Temporary version source for Synclab CICD.
// AirLens uses Kotlin DSL (build.gradle.kts), while the current framework version writer
// rewrites Gradle version declarations using Groovy syntax. Keep this file until the
// framework can preserve/update Kotlin DSL syntax directly, then move versionName/versionCode
// back into android/app/build.gradle.kts and point synclab-release.json there.

versionName "18.0.0.0"
versionCode 1800000000
